# 智造工厂 — GameHMI

一个把 C# 上位机编程教程做成沙盒游戏的教学平台。

写真实 C# 代码控制虚拟工厂设备，在解决问题的过程中学会编程。从零基础到系统架构师。

## 安装

1. 下载 `智造工厂_v1.0.zip` 解压到任意位置
2. 双击 `GameHMI.exe` 运行
3. 不需要安装 .NET，不需要装任何东西。Windows 10/11 64位即可。

---

## 全部关卡

### 第一幕 · 产线学徒（关 01-20）

| 关 | 标题 | 学什么 |
|----|------|--------|
| 01 | 上班第一天 | `Print()`、字符串、变量引用 |
| 02 | 温度好像有点高 | 变量声明、`double`、`$` 字符串插值 |
| 03 | 温度又涨了 | `if/else`、`>` 比较运算符、bool |
| 04 | 不止一个温度点 | 数组 `double[]`、`for` 循环、`Length` |
| 05 | 同样的活每天做 | `void` 方法定义、方法调用、代码复用 |
| 06 | 多温度点管理 | `List<T>`、`.Add()`、`foreach`、`Count` |
| 07 | 不同报警等级 | `else if`、多条件判断、`>=` `<=` |
| 08 | 要看历史趋势 | `while` 循环、计数器、`++` 自增 |
| 09 | 不只温度，还有压力 | `class` 定义、字段 field、`new` 实例化 |
| 10 | 配方参数 | 属性 `get/set`、构造函数、只读属性 |
| 11 | 多台烘干箱同时工作 | `List<对象>`、`foreach`、批量管理 |
| 12 | 数据类型不对 | `double.Parse()`、类型转换、`Convert.ToDouble()` |
| 13 | 五花八门的数据格式 | `Split()`、`Replace()`、字符串解析 |
| 14 | 别让程序崩溃 | `try/catch`、异常处理、容错 |
| 15 | 多台设备有公有私 | `static` 静态字段 vs 实例字段 |
| 16 | 不同设备类型 | `interface` 接口、多态、`: ICheckable` |
| 17 | 传送带速度可调 | 方法重载 `overload`、同名不同参 |
| 18 | 设备运行模式 | `enum` 枚举、`switch/case`、`break` |
| 19 | 数据要留着看 | CSV 格式化、数据导出、for 实践 |
| 20 | 🏆 阶段考核：产线一键巡检 | 综合：类/List/foreach/if/方法封装 |

### 第二幕 · 设备调试员（关 21-35）

| 关 | 标题 | 学什么 |
|----|------|--------|
| 21 | 设备不响应了 | `SerialPort`、`Open()`、`ReadLine()`、波特率 |
| 22 | 发指令控制设备 | `WriteLine()`、指令协议、请求-响应 |
| 23 | 收到的数据乱七八糟 | `Split(',')`、数组索引、数据提取 |
| 24 | 数据一直来 | `while` 轮询读取、`null` 判断、事件概念 |
| 25 | 校验不通过 | 数据校验、异常值过滤、阈值判断 |
| 26 | 多台设备共用串口 | `Dictionary<K,V>`、RS485 轮询、多从站 |
| 27 | 通讯时好时坏 | 超时重试、双层 for 循环、容错 |
| 28 | 字节级数据帧 | 帧格式 `[FRM]`、帧头校验、`StartsWith()` |
| 29 | 不同厂家不同协议 | 协议适配、`StartsWith` 分发、方法封装 |
| 30 | 二进制温度解析 | 十六进制 `Convert.ToInt32(str,16)`、字节运算 |
| 31 | 通讯日志很重要 | 日志记录、序号格式化、采集统计 |
| 32 | 不同串口不同参数 | 串口参数、方法参数、参数校验 |
| 33 | 别让界面卡住 | 异步概念、非阻塞、容错遍历 |
| 34 | 多设备同时采集 | `List` 批量存储、数据统计 |
| 35 | 🏆 阶段考核：多设备监控 | 综合：轮询/解析/异常过滤/报表 |

### 第三幕 · 自动化工程师（关 36-50）

| 关 | 标题 | 学什么 |
|----|------|--------|
| 36 | 认识 PLC 和 Modbus | `Plc.ReadHoldingRegister()`、寄存器、Modbus |
| 37 | PLC 控制电机 | `WriteCoil()`、线圈控制、读写分离 |
| 38 | 传送带联动控制 | 传感器联动、条件控制、自动化逻辑 |
| 39 | 分拣系统 | 工业分拣、寄存器判断、多路控制 |
| 40 | 计数器 | 累加写回、生产计数 |
| 41 | 多台 PLC 管理 | `Dictionary` 分区、多 PLC 管理 |
| 42 | 配方一键切换 | 配方 `Recipe` 类、批量写入 |
| 43 | 报警来了不能全停 | 多级报警、分级处理、降功率/停机 |
| 44 | 设备数据缓存 | 缓存策略、变量比较、去重 |
| 45 | 生产数据要存档 | `ProductionRecord` 类、List 存档、报表统计 |
| 46 | 查历史数据 | 条件查询、WHERE 过滤 |
| 47 | 实时数据趋势 | 趋势分析、差值计算、连续采集 |
| 48 | 批量配置参数 | 批量写入、验证机制 |
| 49 | 定时自动巡检 | 定时任务、自动巡检、运行率 |
| 50 | 🏆 阶段考核：全自动产线 | 综合：多工位/自动控制/产量报表 |

### 第四幕 · 系统架构师（关 51-60）

| 关 | 标题 | 学什么 |
|----|------|--------|
| 51 | 设备类型越来越多 | `abstract` 抽象类、继承、多态 |
| 52 | 设备配置化 | 配置驱动、工厂方法、数据代码分离 |
| 53 | 并发访问要加锁 | `lock` 概念、竞态条件、标志位保护 |
| 54 | 设备多了初始化慢 | 批量初始化、任务列表、并发概念 |
| 55 | 界面和后台要分离 | 回调模式 `Action<T>`、UI 线程分离 |
| 56 | 程序要一直跑不能崩 | `try/catch` 全覆盖、7×24 可靠性 |
| 57 | 不停机热更新 | 热更新、配置对比、在线变更 |
| 58 | 统一协议接口 | 策略模式、`interface` 隔离、协议统一 |
| 59 | 操作日志与审计 | 审计日志、操作留痕、追溯 |
| 60 | 🏆 阶段考核：可扩展框架 | 综合：interface/abstract/concrete/config |

### 第五幕 · 产线指挥官（关 61-65）

| 关 | 标题 | 学什么 |
|----|------|--------|
| 61 | 完整产线联调 | 五段流水线、工段协作、状态机 |
| 62 | 异常处理与恢复 | 故障恢复、重试机制、隔离降级 |
| 63 | 多产线并行调度 | 优先级调度、资源分配 |
| 64 | 生产报表自动生成 | KPI 计算、数据汇总、月报 |
| 65 | 🏆 交付与验收：总控系统 | 终极考核：3线×3段/巡检/报表 |

### Bonus 挑战

| 关 | 标题 | 挑战 |
|----|------|------|
| B1 | 最短代码挑战 | 用最少行数完成巡检 |
| B2 | 容错极限挑战 | 10台设备3台异常，程序不崩 |
| B3 | 极速挑战 | 20台设备最高效轮询 |
| B4 | 沙盒自由模式 | 无限制自由编程，你的工厂 |

---

## 技术栈

| 组件 | 技术 |
|------|------|
| 界面 | .NET 9 WPF |
| 代码编辑器 | AvalonEdit + 40+ 词 IntelliSense |
| 代码执行 | Roslyn CSharpScript 独立进程沙箱 |
| 关卡数据 | JSON (System.Text.Json) |
| 虚拟设备 | 温度传感器、电机、加热器、串口、PLC/Modbus |
| 构建测试 | TestRunner 68/68 关卡自动验证 |

## 开发

```bash
# 构建
dotnet build CodeRunner
dotnet build GameHMI

# 运行测试（68/68 通过）
cd TestRunner && dotnet run

# 打包（自包含，免装 .NET）
dotnet publish -c Release -r win-x64 --self-contained
```

## License

MIT
